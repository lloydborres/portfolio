services:
  portfolio:
    image: portfolio
    container_name: portfolio
    restart: unless-stopped
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 5173:5173
    volumes:
      - .:/app
      - /app/node_modules
